// next config
// web/next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // NÃO desligue o appDir. Deixe o padrão (true).
  // experimental: { appDir: true },
};

export default nextConfig;